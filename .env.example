# Copy this file to .env and fill in your values
# Never commit .env to version control

# Database
DATABASE_DB=example
DATABASE_USER=root
DATABASE_PASSWORD=changeme
DATABASE_HOST=db
DATABASE_PORT=3306

# MySQL root password (used by MariaDB container)
MYSQL_ROOT_PASSWORD=changeme

# Node environment
NODE_ENV=production
